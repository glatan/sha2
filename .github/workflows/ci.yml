name: ci

on: [push, pull_request]

jobs:
  test:
    name: deno test
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL https://deno.land/x/install/install.sh | sh
      - run: deno --version
      - run: deno test
